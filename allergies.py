from math import log2

class Allergies:

    def __init__(self, score):
        self.score = score
        for j in [2**n for n in range(int(log2(self.score)))+1]:
            if
        pass

    def allergic_to(self, item):
        pass

    @property
    def lst(self):
        allergenes={
        "eggs": (1),
        "peanuts": (2),
        "shellfish": (4),
        "strawberries": (8),
        "tomatoes": (16),
        "chocolate": (32),
        "pollen": (64),
        "cats": (128)}


        pass
